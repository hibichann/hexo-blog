<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>错误的v-deep弃用警告</title>
    <url>/CSS/Incorrect-deep-deprecation-warning/</url>
    <content><![CDATA[<p>在 sass/scss 项目中</p>
<p>注意到项目中经常会有关于<code>::v-deep</code>的弃用警告，但是项目里又搜不到<code>::v-deep</code></p>
<p><img src="https://cdn.jsdelivr.net/gh/hibichann/picgo@main/%E8%AD%A6%E5%91%8A.jpg" alt="警告" loading="lazy" class="φbp"></p>
<p><img src="https://cdn.jsdelivr.net/gh/hibichann/picgo@main/%E6%B2%A1%E6%9C%89v-deep.jpg" alt="没有::v-deep" loading="lazy" class="φbp"></p>
<p>然后也去查看了源码里关于这段报错的：</p>
<p><img src="https://cdn.jsdelivr.net/gh/hibichann/picgo@main/202305151015288.png" alt="vdeep判断部分源码" loading="lazy" class="φbp"></p>
<p>打印 n.nodes：</p>
<p><img src="https://cdn.jsdelivr.net/gh/hibichann/picgo@main/202305151017431.png" alt="n.nodes" loading="lazy" class="φbp"></p>
<p>依旧无果，最终在 vuejs 的 issue 中找到:</p>
<p><a href="https://github.com/vuejs/core/issues/4745" target="_blank">Misleading deprecation warning about “::v-deep”, when “:deep” is used in a nested block with SASS</a></p>
<p>实际上是:deep 使用错误，应该是</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line">:<span class="built_in">deep</span>(.ClassName) &#123;</span><br><span class="line">	<span class="comment">// more rules</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>而非</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line">:deep .ClassName &#123;</span><br><span class="line">	<span class="comment">// more rules</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>虽然后者也能工作，但是会错误地触发::v-deep 弃用警告</p>
<p>顺带提了 issue，这个和上述 issue 并不完全重复<br>
<a href="https://github.com/vuejs/core/issues/8313" target="_blank">incorrect warning about “::v-deep”, when “:deep” is wrongly used in a nested block with SASS</a></p>
<blockquote>
<p>2023-07-12:<br>
issue 相关 MR 已被合并，现在该警告样式已经修改</p>
</blockquote>
]]></content>
      <categories>
        <category>CSS</category>
      </categories>
      <tags>
        <tag>Vue3</tag>
        <tag>SASS/SCSS</tag>
      </tags>
  </entry>
  <entry>
    <title>Ant Design的菜单常用配置</title>
    <url>/React/ant-menu-common/</url>
    <content><![CDATA[<p>1.实现手风琴模式<br>
Ant Design 默认没有为菜单组件提供手风琴模式的配置项，需要自己手动实现</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="title function_">handleChange</span> = (<span class="params">Keys: string[]</span>) =&gt; &#123;</span><br><span class="line">	<span class="comment">// 实现手风琴</span></span><br><span class="line">	<span class="title function_">setOpenKeys</span>([<span class="title class_">Keys</span>[<span class="title class_">Keys</span>.<span class="property">length</span> - <span class="number">1</span>]])</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>把该函数绑定在 menu 的 <code>onOpenChange</code>回调函数上，原理是该事件会传递一个已打开的菜单项的<code>key</code>的数组，而且是 push 进去的，数组的最后一项总是最新打开的一项，所以可以把<code>openkeys</code>设置为该数组的最后一项</p>
<p>2.在二级菜单下刷新页面时保留菜单选中和展开</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> firstOpen = <span class="string">&quot;&quot;</span></span><br><span class="line"><span class="comment">// 菜单的key设置为路由地址如&#x27;/user/info&#x27;</span></span><br><span class="line"><span class="comment">// 获取当前Url地址</span></span><br><span class="line"><span class="keyword">const</span> location = <span class="title function_">useLocation</span>()</span><br><span class="line"><span class="comment">//用于寻找key和当前页面地址对应项</span></span><br><span class="line"><span class="keyword">const</span> <span class="title function_">findkey</span> = (<span class="params">obj: ItemType</span>) =&gt; obj?.<span class="property">key</span> === location.<span class="property">pathname</span></span><br><span class="line">items.<span class="title function_">forEach</span>(<span class="function">(<span class="params">i: any</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="comment">// 若菜单项的children不为空数组，去查找二级菜单中是否有对应的key</span></span><br><span class="line">	<span class="keyword">if</span> (i?.<span class="property">children</span> &amp;&amp; i?.<span class="property">children</span>.<span class="property">length</span> &gt; <span class="number">1</span> &amp;&amp; i?.<span class="property">children</span>.<span class="title function_">find</span>(findkey)) &#123;</span><br><span class="line">		firstOpen = i.<span class="property">key</span></span><br><span class="line">		<span class="variable language_">console</span>.<span class="title function_">log</span>(firstOpen)</span><br><span class="line">	&#125;</span><br><span class="line">&#125;)</span><br><span class="line"><span class="keyword">const</span> [openKeys, setOpenKeys] = <span class="title function_">useState</span>([firstOpen] <span class="keyword">as</span> string[])</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>React</category>
      </categories>
      <tags>
        <tag>React</tag>
        <tag>Ant Design</tag>
      </tags>
  </entry>
  <entry>
    <title>如何区分浏览器刷新和关闭？</title>
    <url>/JavaScript/distinguish-between-browser-refresh-and-close/</url>
    <content><![CDATA[<h4 id="起因">起因<a title="#起因" href="#起因"></a></h4>
<p>起因呢是群里有人提问说如何区分浏览器刷新和关闭的事件，他需求在关闭浏览器时清空所有 localStorage 的数据，因为 uhhh————很奇怪，他们的项目要求不能使用 sessionStorage，只能使用 localStorage。</p>
<h4 id="所以这篇文章讨论的是如何在不使用-sessionstorage-的情况下，区分并实现浏览器刷新和关闭的事件。">所以这篇文章讨论的是如何在不使用 sessionStorage 的情况下，区分并实现浏览器刷新和关闭的事件。<a title="#所以这篇文章讨论的是如何在不使用-sessionstorage-的情况下，区分并实现浏览器刷新和关闭的事件。" href="#所以这篇文章讨论的是如何在不使用-sessionstorage-的情况下，区分并实现浏览器刷新和关闭的事件。"></a></h4>
<p>我一开始想到的是<code>onbeforeunload</code>或者<code>unload</code>事件，但是这个事件在浏览器关闭和刷新时都会触发的，<br>
而且各个浏览器厂商的加载流程也不近相同，所以这个事件并不能用来区分浏览器刷新和关闭。<br>
也包括其参数中的<code>e.currentTarget.performance.navigation.type</code>也是一样的。</p>
<p>这个类型可以理解为当前的导航类型，而非下一步操作的类型，所以无法区分刷新和关闭。<br>
他可以确定你是第一次加载或者是刷新所进来的方式（之前的类型），但是无法确定你将要刷新还是关闭（下一步的操作）。<br>
然后是几个可行的方法：</p>
<h4 id="心跳包">心跳包<a title="#心跳包" href="#心跳包"></a></h4>
<p>1.在页面上，添加 <code>onunload</code> 事件（伪代码）：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">myUnload</span>(<span class="params">event</span>) &#123;</span><br><span class="line">	<span class="keyword">if</span> (<span class="variable language_">window</span>.<span class="property">localStorage</span>) &#123;</span><br><span class="line">		<span class="comment">// 标记页面正在被卸载</span></span><br><span class="line">		<span class="variable language_">window</span>.<span class="property">localStorage</span>[<span class="string">&quot;myUnloadEventFlag&quot;</span>] = <span class="keyword">new</span> <span class="title class_">Date</span>().<span class="title function_">getTime</span>()</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//通知服务器我们想在几秒钟内断开用户的连接</span></span><br><span class="line">	<span class="title function_">askServerToDisconnectUserInAFewSeconds</span>()</span><br><span class="line">	<span class="comment">// 异步 AJAX call</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>2.在您的页面上，将 <code>onload</code> 添加到 body（伪代码）：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">myLoad</span>(<span class="params">event</span>) &#123;</span><br><span class="line">	<span class="keyword">if</span> (<span class="variable language_">window</span>.<span class="property">localStorage</span>) &#123;</span><br><span class="line">		<span class="keyword">var</span> t0 = <span class="title class_">Number</span>(<span class="variable language_">window</span>.<span class="property">localStorage</span>[<span class="string">&quot;myUnloadEventFlag&quot;</span>])</span><br><span class="line">		<span class="keyword">if</span> (<span class="built_in">isNaN</span>(t0)) t0 = <span class="number">0</span></span><br><span class="line">		<span class="keyword">var</span> t1 = <span class="keyword">new</span> <span class="title class_">Date</span>().<span class="title function_">getTime</span>()</span><br><span class="line">		<span class="keyword">var</span> duration = t1 - t0</span><br><span class="line">		<span class="keyword">if</span> (duration &lt; <span class="number">10</span> * <span class="number">1000</span>) &#123;</span><br><span class="line">			<span class="comment">// less than 10 seconds since the previous Unload event =&gt; it&#x27;s a browser reload (so cancel the disconnection request)</span></span><br><span class="line">			<span class="title function_">askServerToCancelDisconnectionRequest</span>() <span class="comment">// asynchronous AJAX call</span></span><br><span class="line">		&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">			<span class="comment">// last unload event was for a tab/window close =&gt; do whatever you want (I do nothing here)</span></span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>3.在服务器上，将断开连接请求收集到列表中，并设置一个计时器线程，该线程定期检查列表（我每 20 秒使用一次）。断开连接请求超时（即 5 秒消失）后，断开用户与服务器的连接。如果同时收到断开连接请求取消，则会从列表中删除相应的断开连接请求，以便用户不会断开连接。</p>
<h4 id="鼠标事件">鼠标事件<a title="#鼠标事件" href="#鼠标事件"></a></h4>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">$(<span class="variable language_">window</span>).<span class="title function_">on</span>(<span class="string">&quot;mouseover&quot;</span>, <span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">	<span class="variable language_">window</span>.<span class="property">onbeforeunload</span> = <span class="literal">null</span></span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line">$(<span class="variable language_">window</span>).<span class="title function_">on</span>(<span class="string">&quot;mouseout&quot;</span>, <span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">	<span class="variable language_">window</span>.<span class="property">onbeforeunload</span> = <span class="title class_">ConfirmLeaveMessage</span></span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">ConfirmLeaveMessage</span>(<span class="params"></span>) &#123;</span><br><span class="line">	<span class="comment">//return undefined;</span></span><br><span class="line">	<span class="keyword">return</span> <span class="string">&quot;Are you sure you want to leave this page without saving?&quot;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>老实讲目前除了 sessionStorage 原生支持，还没有其他可靠的方案去判断浏览器为刷新或关闭，后续会继续更新本篇文章。</p>
]]></content>
      <categories>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>初识Vue3</title>
    <url>/Vue/first-vue3/</url>
    <content><![CDATA[<h4 id="首次学习-vue3-时的笔记">首次学习 vue3 时的笔记<a title="#首次学习-vue3-时的笔记" href="#首次学习-vue3-时的笔记"></a></h4>
<p>要从单个绑定获取多个 ref，请将  <code>ref</code>  绑定到一个更灵活的函数上：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">v-for</span>=<span class="string">&quot;item in list&quot;</span> <span class="attr">:ref</span>=<span class="string">&quot;setItemRef&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p><code>$attrs</code>  现在包含了所有传递给组件的 <code>attribute</code>，包括  <code>class</code>  和  <code>style</code>。</p>
<p><code>ref()</code>用于基本类型 <code>reactive()</code>用于对象或数组 实现深层的响应式数据</p>
<p>具名插槽 <code>slot=“qwe” </code>&gt;&gt;&gt;&gt;<code> v-solt:qwe</code></p>
<p>自定义事件需要用<code>emits</code>接受</p>
<p><code>setup</code>会接受两个参数</p>
<p><code>props</code>接收后为 setup 第一个参数</p>
<p><code>context </code></p>
<p><code>attrs</code>值为对象，包含了未被<code>props</code>接受的来自父组件中的属性，包括<code>class</code>和<code>style</code></p>
<p><code>slots</code>收到的插槽内容</p>
<p><code>emit</code>分发自定义事件的函数，用于触发自定义事件</p>
<p><code>expose</code> 指定组件向父组件暴露出去的公共函数</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="title function_">watch</span>(prop, <span class="title function_">callback</span>(newValue,oldValue)=&gt;&#123;</span><br><span class="line">    ...</span><br><span class="line">&#125;, &#123;immediate：<span class="literal">true</span>,<span class="attr">deep</span>:<span class="literal">true</span>&#125;)</span><br></pre></td></tr></table></figure>
<h6 id="watch-监听对象">watch 监听对象<a title="#watch-监听对象" href="#watch-监听对象"></a></h6>
<p>1 监听<code>reactive</code>定义的对象时 <code>oldValue</code>无法正确获取，</p>
<p>2 监听<code>reactive</code>定义的对象时 强制开启了<code>deep</code>深层监视，无法关闭</p>
<p>3 监听<code>reactive</code>定义对象中的某个属性 ，<code>prop</code>不能直接写<code>ob1.prop</code>需要写成返回值的样式 <code>()=&gt;ob1.prop</code></p>
<p>4 监听对象中的深层属性如<code>ob1.job</code>，需要开启<code>deep</code></p>
<p><code>watchEffect</code>默认开启<code>immediate</code>，只监视回调函数中被使用的值</p>
<h6 id="生命周期">生命周期<a title="#生命周期" href="#生命周期"></a></h6>
<p>创建前后&gt;&gt;<code> setup()</code></p>
<p>挂载前后<code>onBeforeMount</code>/<code>onMounted</code></p>
<p>更新前后<code>onBeforeUpdated</code>/<code>onUpdated</code></p>
<p>卸载前后<code>onBeforeUnmount</code>/<code>onUnmounted</code></p>
<h6 id="自定义-hook">自定义 hook<a title="#自定义-hook" href="#自定义-hook"></a></h6>
<p><code>toRef </code>创建一个 ref 对象，其 value 值指向另一个对象的中的某个属性</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> name = <span class="title function_">toRef</span>(person, <span class="string">&quot;name&quot;</span>)</span><br></pre></td></tr></table></figure>
<p><code>toRefs</code>可以展开一个 reactive 生成的 proxy 对象，让它成为一个普通对象，但该对象中的所有属性都是一个 ref 对象</p>
<h6 id="移除了.native">移除了.<code>native</code><a title="#移除了.native" href="#移除了.native"></a></h6>
<p>想要为组件绑定如 click 这种原生事件时，只需注意不要在 emits 中接受即可，即可防止 click 被视为自定义事件，相同的，若要重载 click 事件，在 emits 中接收即可</p>
<p>移除过滤器</p>
<p><code>fragment </code>组件模板中可以不写根元素，减少了性能开销</p>
<p><code>teleport</code>组件可以将某一部分元素输送到某个指定元素中，更方便定位。</p>
<h6 id="异步组件">异步组件<a title="#异步组件" href="#异步组件"></a></h6>
<p>使用<code>defineAsyncComponent</code>来引入一个异步组件，</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123;defineAsyncComponent&#125; <span class="keyword">from</span> <span class="string">&#x27;vue&#x27;</span></span><br><span class="line"><span class="keyword">const</span> <span class="title class_">Child</span>=<span class="title function_">defineAsyncComponent</span>(<span class="function">()=&gt;</span><span class="keyword">import</span>(<span class="string">&#x27;./Components/Child&#x27;</span>)</span><br></pre></td></tr></table></figure>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">suspense</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">template</span> #<span class="attr">default</span>&gt;</span></span><br><span class="line">		<span class="comment">&lt;!--此处放置真正的组件--&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">todo-list</span> /&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">template</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">template</span> #<span class="attr">fallback</span>&gt;</span></span><br><span class="line">		<span class="comment">&lt;!--此处放置骨架屏/渲染期间的显示--&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">div</span>&gt;</span>Loading...<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">template</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">suspense</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p><code>shallowReactive</code>只有第一层数据拥有响应式</p>
<p>如同</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> ob1=<span class="title function_">shallowReactive</span>(&#123;</span><br><span class="line">    <span class="attr">name</span>:<span class="string">&quot;zhangsan&quot;</span>,</span><br><span class="line">    <span class="attr">age</span>:<span class="number">18</span>,</span><br><span class="line">    <span class="attr">job</span>:&#123;</span><br><span class="line">        <span class="attr">j1</span>:<span class="string">&quot;stuff&quot;</span></span><br><span class="line">        <span class="attr">salary</span>:<span class="number">20</span> <span class="comment">// 深层次的数据将会是一个普通的对象</span></span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>
<p>这样的数据，采用 shallowReactive 无法令 j1 和 salary 拥有响应式</p>
<p><code>shallowRef</code>对于基本类型等同于 ref，对于对象类型，不会添加响应式</p>
<p>readonly 只读</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">ob1 = <span class="title function_">readonly</span>(ob1)</span><br></pre></td></tr></table></figure>
<p><strong><em>采用 readonly 不会令数据丢失响应式，但是会令数据不可更改</em></strong></p>
<p><code>shallowReadonly</code>浅只读</p>
<p>仅令 ob1 中的 name 和 age 不可更改，job 内部的属性可以更改</p>
<p><code>toRaw</code>令一个 reactive 数据失去响应式</p>
<p><code>markRaw</code>令一个 reactive 数据永远不能转为响应式</p>
<p><strong><em>isRef:检查一个值是否为一个 ref 对象</em></strong><br>
<strong><em>lsReactive:检查一个对象是否是由 reactive 创建的响应式代理</em></strong><br>
<strong><em>isReadonly:检查—个对象是否是由 readonly 创建的只读代理<br>
isProxy:检查一个对象是否是由 reactive 或者 readonly 方法创建的代理</em></strong></p>
<p><code>provide</code>/<code>inject</code>适用于祖孙通信</p>
<p>祖先组件无需指名传值给哪个后代，后代也无需声明数据来源</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="title function_">provide</span>(name, data)</span><br></pre></td></tr></table></figure>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="title function_">inject</span>(name)</span><br></pre></td></tr></table></figure>
<p>customRef 自定义 ref</p>
<hr>
]]></content>
      <categories>
        <category>Vue</category>
      </categories>
      <tags>
        <tag>Vue3</tag>
      </tags>
  </entry>
  <entry>
    <title>阻止表单的默认行为时的行为</title>
    <url>/JavaScript/form-default/</url>
    <content><![CDATA[<p>想象有一个这样的表单：</p>
<p><img src="https://cdn.jsdelivr.net/gh/hibichann/picgo@main/202306150929505.png" alt="form-default" loading="lazy" class="φbp"></p>
<p>我们很多时候，并不想在表单提交以后刷新页面，就需要阻止默认的提交行为<br>
此时如果在 input 输入框输入之后按下 enter 会触发哪个函数？<br>
会触发整个表单中的第一个按钮？或者 input 输入框后的第一个按钮？再或者是具有<code>type=&quot;submit&quot;</code>的按钮？<br>
答案是会触发整个表单的第一个按钮，无论多少个输入框，总会触发整个表单的第一个按钮</p>
]]></content>
      <categories>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>hexo在网页中插入一段自定义的代码</title>
    <url>/JavaScript/hexo-custom-plugin/</url>
    <content><![CDATA[<p>在 hexo 根目录下创建文件夹<code>scripts</code>，然后写自己的代码进去<br>
有时我们想要修改主题的默认样式，但是主题是<code>npm install</code>安装的，不在<code>themes</code>目录下，也可通过这种方式注入一段 js</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">hexo.<span class="property">extend</span>.<span class="property">filter</span>.<span class="title function_">register</span>(<span class="string">&quot;after_render:html&quot;</span>, <span class="keyword">function</span> (<span class="params">htmlContent</span>) &#123;</span><br><span class="line">	<span class="comment">// 在此处输入你想要注入的 CSS 样式代码</span></span><br><span class="line">	<span class="keyword">var</span> css = <span class="string">`.φbm &#123;</span></span><br><span class="line"><span class="string">        max-height: 500px;</span></span><br><span class="line"><span class="string">        object-fit: cover;</span></span><br><span class="line"><span class="string">        filter:brightness(0.95);</span></span><br><span class="line"><span class="string">        transition:all 0.2s;</span></span><br><span class="line"><span class="string">    &#125;</span></span><br><span class="line"><span class="string">    .φbm:hover&#123;</span></span><br><span class="line"><span class="string">        filter:unset;</span></span><br><span class="line"><span class="string">        transition:all 0.2s;</span></span><br><span class="line"><span class="string">        transform: scale(1.05);</span></span><br><span class="line"><span class="string">    &#125;</span></span><br><span class="line"><span class="string">    `</span></span><br><span class="line">	<span class="comment">// 将 CSS 样式代码插入到 HTML 中</span></span><br><span class="line">	<span class="keyword">return</span> htmlContent.<span class="title function_">replace</span>(<span class="regexp">/&lt;\/head&gt;/i</span>, <span class="string">&quot;&lt;style&gt;&quot;</span> + css + <span class="string">&quot;&lt;/style&gt;&lt;/head&gt;&quot;</span>)</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>
<p>这段代码就可以自动的被 hexo 注入到项目中，具体参考 hexo 插件手册</p>
]]></content>
      <categories>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>CSS</tag>
        <tag>HTML</tag>
      </tags>
  </entry>
  <entry>
    <title>Javascript函数柯里化</title>
    <url>/JavaScript/javascript-function-curry/</url>
    <content><![CDATA[<p>函数柯里化就是接受多个参数的函数变换成接受一个单一参数(最初函数的第一个参数)的函数,并且返回接受余下的参数且返回结果的新函数的函数。<br>
听起来有点绕，对吧？换句话就是：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="title function_">fn</span>(a, b, c)</span><br><span class="line"><span class="keyword">const</span> curryFn = <span class="title function_">curry</span>(fn)</span><br><span class="line"><span class="title function_">fn</span>(a, b, c) === <span class="title function_">curryFn</span>(a)(b)(c)</span><br></pre></td></tr></table></figure>
<p>我们要做的就是实现这个<code>curry</code>函数，它不会调用函数，它只是对函数做了一次转换</p>
<p>先来一个简单的例子：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">add</span>(<span class="params">x, y</span>) &#123;</span><br><span class="line">	<span class="keyword">return</span> x + y</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">const</span> curryAdd = <span class="title function_">curry</span>(add)</span><br><span class="line"><span class="keyword">function</span> <span class="title function_">curry</span>(<span class="params">fn</span>) &#123;</span><br><span class="line">	<span class="keyword">return</span> <span class="function">(<span class="params">x</span>) =&gt;</span> &#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="function">(<span class="params">y</span>) =&gt;</span> &#123;</span><br><span class="line">			<span class="keyword">return</span> <span class="title function_">fn</span>(x, y)</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>但这样的 curry 函数局限过大，那如何封装一个通用的 curry 函数呢？</p>
<p>首先有两个目标，首先要确定函数执行的时机，然后就是参数收集的时机，如果参数没有收集完成，就继续收集和汇总参数。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">curry</span>(<span class="params">fn</span>) &#123;</span><br><span class="line">	<span class="keyword">return</span> <span class="keyword">function</span> <span class="title function_">curried</span>(<span class="params">...args</span>) &#123;</span><br><span class="line">		<span class="keyword">if</span> (args.<span class="property">length</span> &gt;= fn.<span class="property">length</span>) &#123;</span><br><span class="line">			<span class="keyword">return</span> fn.<span class="title function_">apply</span>(<span class="variable language_">this</span>, args)</span><br><span class="line">		&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">			<span class="keyword">return</span> <span class="keyword">function</span> (<span class="params">...args2</span>) &#123;</span><br><span class="line">				<span class="keyword">return</span> curried.<span class="title function_">apply</span>(<span class="variable language_">this</span>, args.<span class="title function_">concat</span>(args2))</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><code>curry</code> 函数接受一个普通函数 <code>fn</code> 并返回一个新的函数 <code>curried。当</code> <code>curried</code> 被调用时，它会检查传递给它的参数数量是否足够调用 <code>fn</code>。如果足够，<code>fn</code> 就会被应用并返回结果。否则，<code>curried</code> 会返回一个新的函数，该函数使用 <code>args.concat(args2)</code> 将之前传递的参数和新传递的参数合并，然后再次调用 <code>curried</code></p>
<p>在这个过程中，每个返回的新函数都具有对原始函数 <code>fn</code> 的引用，并且可以收集任意数量的参数。最终，当最后一个新函数被调用并传入了所有需要的参数时，<code>fn</code> 被调用并返回结果。</p>
<p>通过使用柯里化，我们可以创建更加灵活和重复使用的函数。</p>
]]></content>
      <categories>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>JS总能够保证对象顺序么</title>
    <url>/JavaScript/js-object-sort/</url>
    <content><![CDATA[<p>当你创建了一个对象类似于：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> obj = &#123;&#125;</span><br><span class="line">obj.<span class="property">prop1</span> = <span class="string">&quot;Foo&quot;</span></span><br><span class="line">obj.<span class="property">prop2</span> = <span class="string">&quot;Bar&quot;</span></span><br></pre></td></tr></table></figure>
<p>那它是否总是能表现为：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&#123; prop1 : <span class="string">&quot;Foo&quot;</span>, prop2 : <span class="string">&quot;Bar&quot;</span> &#125;</span><br></pre></td></tr></table></figure>
<p>换句话说，js 是否总能保证对象的顺序？</p>
<p>答案是能够保证，但不一定是插入顺序（ES 2015 之前）</p>
<p>大部分浏览器都遵循这样的迭代规则：</p>
<blockquote>
<p>1.正整数键按升序排列（以及像“1”这样的字符串被解析为整数）</p>
<p>2.字符串键，按插入顺序（ES 2015 保证这一点，所有浏览器都遵守）</p>
<p>3.符号名称，按插入顺序排列（ES 2015 保证所有浏览器都符合此要求）</p>
</blockquote>
<p>一些较旧的浏览器采用的是前两种，按插入顺序迭代所有键。如果键可以被解析为整数，那么最好不要依赖于任何特定的迭代顺序。</p>
<p>当前语言规范（ES 2015 及以后）中，插入顺序被保留，除非键被解析为正整数（包括“7”或“99”这样字符串包裹的整数），这种情况下行为因浏览器而异。比如，Chrome/V8 在键解析为数字时将不遵守插入顺序。</p>
<h4 id="demo:">Demo:<a title="#demo:" href="#demo:"></a></h4>
<p><img src="https://cdn.jsdelivr.net/gh/hibichann/picgo@main/202305161056090.png" alt="image-20230516105647021" loading="lazy" class="φbp"></p>
<p><img src="https://cdn.jsdelivr.net/gh/hibichann/picgo@main/202305161057834.png" alt="image-20230516105709791" loading="lazy" class="φbp"></p>
<p>在 chrome 中，整数键不遵守插入顺序，而是升序。</p>
<h4 id="后记">后记<a title="#后记" href="#后记"></a></h4>
<p>这是一个很好的，现有的约定俗成的行为驱动规范完善的例子。es2015 之前并没有规范，但是 es2015 根据多数浏览器的实现制定了规则。<br>
但我们开发时并不能确定所处的运行环境，单就对象中 key 的顺序而言，最好不要依赖规范，而是自行遍历再过滤 key。或者采用 map 来保证 key 的顺序固定，map 中的键是有序的，map 在遍历的时候保证会按照插入顺序来迭代</p>
]]></content>
      <categories>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>在 element-plus 中保持菜单展开</title>
    <url>/Vue/keep-menu-open-in-element-plus/</url>
    <content><![CDATA[<p>在 element plus 中，有时我们需要始终保持菜单的展开状态（如单独的全展开的分类页），而在 element plus 中并未提供相应的属性。</p>
<p>但是可以首先为<code>el-menu</code>设置<code>default-openeds</code> 属性，值为需要展开的菜单。<br>
然后直接在<code>el-menu</code>的属性中，为菜单创建<code>ref</code>实例，并绑定<code>close</code>方法，在<code>close</code>方法中调用菜单实例身上的<code>open</code>方法来再次展开这个菜单。</p>
<p>​ 需要保持菜单展开的情况许多时候还需要去除二级菜单后面的小箭头，可以通过为<code>.el-sub-menu__icon-arrow</code>设置 <code>display: none !important;</code> 来清除二级菜单后的按钮。</p>
<p>模板：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">el-menu</span></span></span><br><span class="line"><span class="tag">	<span class="attr">class</span>=<span class="string">&quot;el-menu-demo&quot;</span></span></span><br><span class="line"><span class="tag">	<span class="attr">:ellipsis</span>=<span class="string">&quot;false&quot;</span></span></span><br><span class="line"><span class="tag">	<span class="attr">:default-openeds</span>=<span class="string">&quot;[&#x27;2&#x27;, &#x27;2-4&#x27;]&quot;</span></span></span><br><span class="line"><span class="tag">	@<span class="attr">close</span>=<span class="string">&quot;keepopen&quot;</span></span></span><br><span class="line"><span class="tag">	<span class="attr">ref</span>=<span class="string">&quot;menuRef&quot;</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">el-menu-item</span> <span class="attr">index</span>=<span class="string">&quot;0&quot;</span>&gt;</span>LOGO<span class="tag">&lt;/<span class="name">el-menu-item</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;flex-grow&quot;</span> /&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">el-menu-item</span> <span class="attr">index</span>=<span class="string">&quot;1&quot;</span>&gt;</span>Processing Center<span class="tag">&lt;/<span class="name">el-menu-item</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">el-sub-menu</span> <span class="attr">index</span>=<span class="string">&quot;2&quot;</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">template</span> #<span class="attr">title</span>&gt;</span>Workspace<span class="tag">&lt;/<span class="name">template</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">el-menu-item</span> <span class="attr">index</span>=<span class="string">&quot;2-1&quot;</span>&gt;</span>item one<span class="tag">&lt;/<span class="name">el-menu-item</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">el-menu-item</span> <span class="attr">index</span>=<span class="string">&quot;2-2&quot;</span>&gt;</span>item two<span class="tag">&lt;/<span class="name">el-menu-item</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">el-menu-item</span> <span class="attr">index</span>=<span class="string">&quot;2-3&quot;</span>&gt;</span>item three<span class="tag">&lt;/<span class="name">el-menu-item</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">el-sub-menu</span> <span class="attr">index</span>=<span class="string">&quot;2-4&quot;</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">template</span> #<span class="attr">title</span>&gt;</span>item four<span class="tag">&lt;/<span class="name">template</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">el-menu-item</span> <span class="attr">index</span>=<span class="string">&quot;2-4-1&quot;</span>&gt;</span>item one<span class="tag">&lt;/<span class="name">el-menu-item</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">el-menu-item</span> <span class="attr">index</span>=<span class="string">&quot;2-4-2&quot;</span>&gt;</span>item two<span class="tag">&lt;/<span class="name">el-menu-item</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">el-menu-item</span> <span class="attr">index</span>=<span class="string">&quot;2-4-3&quot;</span>&gt;</span>item three<span class="tag">&lt;/<span class="name">el-menu-item</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;/<span class="name">el-sub-menu</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">el-sub-menu</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">el-menu</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>方法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> menuRef = ref &lt; any &gt; <span class="string">&quot;&quot;</span></span><br><span class="line"><span class="keyword">const</span> <span class="title function_">keepopen</span> = (<span class="params">s</span>) =&gt; &#123;</span><br><span class="line">	menuRef.<span class="property">value</span>.<span class="title function_">open</span>(s)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="https://cdn.jsdelivr.net/gh/hibichann/picgo@main/img1.png" alt="效果" loading="lazy" class="φbp"></p>
]]></content>
      <categories>
        <category>Vue</category>
      </categories>
      <tags>
        <tag>Vue3</tag>
        <tag>Element Plus</tag>
      </tags>
  </entry>
  <entry>
    <title>Nginx部署刷新404</title>
    <url>/Vue/nginx-404/</url>
    <content><![CDATA[<p>解决刷新 404 问题：</p>
<ol>
<li>
<p>使用 hash 模式路由</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//vue3</span></span><br><span class="line"><span class="keyword">const</span> router = <span class="title function_">createRouter</span>(&#123;</span><br><span class="line">	<span class="attr">linkActiveClass</span>: <span class="string">&quot;active&quot;</span>,</span><br><span class="line">	<span class="attr">history</span>: <span class="title function_">createWebHistory</span>(),</span><br><span class="line">	routes,</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//vue2</span></span><br><span class="line"><span class="keyword">const</span> router = <span class="keyword">new</span> <span class="title class_">Router</span>(&#123;</span><br><span class="line">	<span class="attr">mode</span>: <span class="string">&quot;hash&quot;</span>,</span><br><span class="line">	<span class="attr">routes</span>: [</span><br><span class="line">		&#123; <span class="attr">path</span>: <span class="string">&quot;/&quot;</span>, <span class="attr">redirect</span>: <span class="string">&quot;/login&quot;</span> &#125;,</span><br><span class="line">		&#123; <span class="attr">path</span>: <span class="string">&quot;/login&quot;</span>, <span class="attr">component</span>: <span class="title class_">Login</span> &#125;,</span><br><span class="line">	],</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>
</li>
<li>
<p>在 nginx 中配置</p>
<figure class="highlight nginx"><table><tr><td class="code"><pre><span class="line"><span class="section">location</span> / &#123;</span><br><span class="line">    <span class="attribute">root</span>   html/dist;</span><br><span class="line">    <span class="attribute">index</span>  index.html index.htm;</span><br><span class="line">    <span class="attribute">try_files</span> <span class="variable">$uri</span> <span class="variable">$uri</span>/ /index.html;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ol>
]]></content>
      <categories>
        <category>Vue</category>
      </categories>
      <tags>
        <tag>Vue</tag>
        <tag>Nginx</tag>
      </tags>
  </entry>
  <entry>
    <title>react中使用...批量传递props</title>
    <url>/React/react-three-dot-props/</url>
    <content><![CDATA[<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">App</span> <span class="keyword">extends</span> <span class="title class_ inherited__">React.Component</span> &#123;</span><br><span class="line">	<span class="title function_">render</span>(<span class="params"></span>) &#123;</span><br><span class="line">		<span class="keyword">const</span> &#123; name1, age &#125; = <span class="variable language_">this</span>.<span class="property">props</span></span><br><span class="line">		<span class="keyword">return</span> (</span><br><span class="line">			<span class="language-xml"><span class="tag">&lt;<span class="name">span</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">				&#123;name1&#125;,&#123;age&#125;</span></span><br><span class="line"><span class="language-xml">			<span class="tag">&lt;/<span class="name">span</span>&gt;</span></span></span><br><span class="line">		)</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">const</span> p = &#123; <span class="attr">name1</span>: <span class="string">&quot;tom&quot;</span>, <span class="attr">age</span>: <span class="number">1</span> &#125;</span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(&#123; ...p &#125;) <span class="comment">//输出对象p</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(...p) <span class="comment">//报错，没有迭代器iterator</span></span><br><span class="line">root.<span class="title function_">render</span>(</span><br><span class="line">	<span class="language-xml"><span class="tag">&lt;<span class="name">h1</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">		<span class="tag">&lt;<span class="name">App</span> &#123;<span class="attr">...p</span>&#125;&gt;</span><span class="tag">&lt;/<span class="name">App</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">		&#123;/* 这里的花括号是react表明内部是js语法，而非es6展开运算符 */&#125;</span></span><br><span class="line"><span class="language-xml">		&#123;/* 此处传入的是name1=&quot;tom&quot;,age=1,而非p对象&#123;name1:&#x27;tom&#x27;,age:1&#125; */&#125;</span></span><br><span class="line"><span class="language-xml">		&#123;/* react为批量传递props提供了...迭代器，但仅能用于传递props */&#125;</span></span><br><span class="line"><span class="language-xml">	<span class="tag">&lt;/<span class="name">h1</span>&gt;</span></span></span><br><span class="line">)</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>React</category>
      </categories>
      <tags>
        <tag>SASS/SCSS</tag>
        <tag>React</tag>
      </tags>
  </entry>
  <entry>
    <title>solidity函数</title>
    <url>/Crypto/solidity-func/</url>
    <content><![CDATA[<p>function <function name>(<parameter types>) {internal|external|public|private} [pure|view|payable] [returns (<return types>)]</p>
<ol>
<li>
<p><code>function</code>：声明函数时的固定用法，想写函数，就要以 function 关键字开头。</p>
</li>
<li>
<p><code>&lt;function name&gt;</code>：函数名。</p>
</li>
<li>
<p><code>(&lt;parameter types&gt;)</code>：圆括号里写函数的参数，也就是要输入到函数的变量类型和名字。</p>
</li>
<li>
<p><code>&#123;internal|external|public|private&#125;</code>：函数可见性说明符，一共 4 种。没标明函数类型的，默认<code>public</code>。合约之外的函数，即&quot;自由函数&quot;，始终具有隐含<code>internal</code>可见性。</p>
<ul>
<li><code>public</code>: 内部外部均可见。</li>
<li><code>private</code>: 只能从本合约内部访问，继承的合约也不能用。</li>
<li><code>external</code>: 只能从合约外部访问（但是可以用<code>this.f()</code>来调用，<code>f</code>是函数名）。</li>
<li><code>internal</code>: 只能从合约内部访问，继承的合约可以用。</li>
</ul>
<p><strong>Note 1</strong>: 没有标明可见性类型的函数，默认为<code>public</code>。</p>
<p><strong>Note 2</strong>: <code>public|private|internal</code>  也可用于修饰状态变量。 <code>public</code>变量会自动生成同名的<code>getter</code>函数，用于查询数值。</p>
<p><strong>Note 3</strong>: 没有标明可见性类型的状态变量，默认为<code>internal</code>。</p>
</li>
<li>
<p><code>[pure|view|payable]</code>：决定函数权限/功能的关键字。<code>payable</code>（可支付的）很好理解，带着它的函数，运行的时候可以给合约转入<code>ETH</code>。</p>
</li>
<li>
<p><code>[returns ()]</code>：函数返回的变量类型和名称。</p>
</li>
</ol>
<p>关于 pure/view：</p>
<p>被 pure 修饰的函数不可读取也不可修改链上数据，即纯粹的函数，函数内部无法直接读到合约内的变量，可以传入参数进行处理</p>
<p>被 view 修饰的函数可以读取链上数据，但是不能修改</p>
<p>默认为 payable 状态，可读可写</p>
<blockquote>
<p>注：实际上 pure/view 修饰的函数 gas 费会更高，猜测原因是由于默认支持链上数据读写，所以减少功能需要额外的标识，提升了 gas 费</p>
</blockquote>
]]></content>
      <categories>
        <category>Crypto</category>
      </categories>
      <tags>
        <tag>ETH</tag>
        <tag>Solidity</tag>
      </tags>
  </entry>
  <entry>
    <title>js中的定时器似乎比实际时间更慢？</title>
    <url>/JavaScript/timer-in-js/</url>
    <content><![CDATA[<p>有时你可能会注意到，你切换到了另一个网页，然后过一会儿再回到当前页面，这个页面上的某个计时器似乎比预期的要慢一些。<br>
难道是开发者肆意为之？想要在你等待计时器的时候，让你多看几眼广告或者多等几秒再开始下载？当然不是，这是因为浏览器的一些优化策略和单进程的js导致的。</p>
<h4 id="1.嵌套超时">1.嵌套超时<a title="#1.嵌套超时" href="#1.嵌套超时"></a></h4>
<p>正如 HTML 标准中规定的那样，一旦对 setTimeout 的嵌套调用被安排了 5<br>
次，浏览器将强制执行 4<br>
毫秒的最小超时。但这个嵌套的次数和超时时间各家浏览器的实可能不尽相同。</p>
<p>这可以在下面的例子中看到，在这个例子中，我们嵌套了对 setTimeout 的调用，延迟为 0<br>
毫秒，并记录每次调用处理程序时的延迟。前四次，延迟约为 0 毫秒，之后约为 4 毫秒：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">button</span> <span class="attr">id</span>=<span class="string">&quot;run&quot;</span>&gt;</span>运行<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">table</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">thead</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">tr</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">th</span>&gt;</span>之前<span class="tag">&lt;/<span class="name">th</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">th</span>&gt;</span>现在<span class="tag">&lt;/<span class="name">th</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">th</span>&gt;</span>实际延时<span class="tag">&lt;/<span class="name">th</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;/<span class="name">tr</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">thead</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">tbody</span> <span class="attr">id</span>=<span class="string">&quot;log&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">tbody</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">table</span>&gt;</span></span><br></pre></td></tr></table></figure>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> last = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">let</span> iterations = <span class="number">10</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">timeout</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="comment">// 记录调用时间</span></span><br><span class="line">  <span class="title function_">logline</span>(<span class="keyword">new</span> <span class="title class_">Date</span>().<span class="title function_">getMilliseconds</span>());</span><br><span class="line">  <span class="comment">// 如果还没结束，计划下次调用</span></span><br><span class="line">  <span class="keyword">if</span> (iterations-- &gt; <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="built_in">setTimeout</span>(timeout, <span class="number">0</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">function</span> <span class="title function_">run</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="comment">// 清除日志</span></span><br><span class="line">  <span class="keyword">const</span> log = <span class="variable language_">document</span>.<span class="title function_">querySelector</span>(<span class="string">&quot;#log&quot;</span>);</span><br><span class="line">  <span class="keyword">while</span> (log.<span class="property">lastElementChild</span>) &#123;</span><br><span class="line">    log.<span class="title function_">removeChild</span>(log.<span class="property">lastElementChild</span>);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 初始化迭代次数和开始时间戳</span></span><br><span class="line">  iterations = <span class="number">10</span>;</span><br><span class="line">  last = <span class="keyword">new</span> <span class="title class_">Date</span>().<span class="title function_">getMilliseconds</span>();</span><br><span class="line">  <span class="comment">// 开启计时器</span></span><br><span class="line">  <span class="built_in">setTimeout</span>(timeout, <span class="number">0</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">logline</span>(<span class="params">now</span>) &#123;</span><br><span class="line">  <span class="comment">// 输出上一个时间戳、新的时间戳及差值</span></span><br><span class="line">  <span class="keyword">const</span> tableBody = <span class="variable language_">document</span>.<span class="title function_">getElementById</span>(<span class="string">&quot;log&quot;</span>);</span><br><span class="line">  <span class="keyword">const</span> logRow = tableBody.<span class="title function_">insertRow</span>();</span><br><span class="line">  logRow.<span class="title function_">insertCell</span>().<span class="property">textContent</span> = last;</span><br><span class="line">  logRow.<span class="title function_">insertCell</span>().<span class="property">textContent</span> = now;</span><br><span class="line">  logRow.<span class="title function_">insertCell</span>().<span class="property">textContent</span> = now - last;</span><br><span class="line">  last = now;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="variable language_">document</span>.<span class="title function_">querySelector</span>(<span class="string">&quot;#run&quot;</span>).<span class="title function_">addEventListener</span>(<span class="string">&quot;click&quot;</span>, run);</span><br></pre></td></tr></table></figure>
<p><img src="https://cdn.jsdelivr.net/gh/hibichann/picgo@main/202308030952843.png" alt="结果" loading="lazy" class="φbp"></p>
<h4 id="2.非活动标签的超时">2.非活动标签的超时<a title="#2.非活动标签的超时" href="#2.非活动标签的超时"></a></h4>
<p>为了优化后台标签的加载损耗（以及降低耗电量），浏览器会在非活动标签中强制执行一个最小的超时延迟。如果一个页面正在使用网络音频<br>
API AudioContext 播放声音，也可以不执行该延迟。</p>
<p>这方面的具体情况与浏览器有关：</p>
<p>Firefox 桌面版和 Chrome 针对不活动标签都有一个 1 秒的最小超时值。 安卓版 Firefox<br>
浏览器对不活动的标签有一个至少 15 分钟的超时，并可能完全卸载它们。<br>
如果标签中包含 AudioContext，Firefox 不会对非活动标签进行节流。</p>
<h4 id="3.单线程的-javascript">3.单线程的 JavaScript<a title="#3.单线程的-javascript" href="#3.单线程的-javascript"></a></h4>
<p>JS 是单线程的，有它独特的事件循环机制。当我们调用 setTimeout<br>
时，浏览器会将回调函数放入事件队列中，等待执行。当主线程执行完毕后，会立即执行事件队列中的回调函数。如果主线程执行时间过长，那么回调函数就会被延迟执行。</p>
<p>事件循环机制的执行顺序:</p>
<blockquote>
<p>1.执行当前执行栈中的顺序<br>
2.当执行栈是空的时候，事件循环会从事件队列中取出一个任务进行执行，该任务是宏任务<br>
3.当宏任务执行完毕后，事件循环会检查是否有微任务需要执行，如果有会依次执行所有微任务，直到微任务队列为空<br>
4.重复执行步骤 2-3，直到事件队列和微任务队列均为空</p>
</blockquote>
<p>所以如果页面（或操作系统/浏览器）正忙于其他任务，超时也可能比预期的晚。需要注意的一个重要情况是，在调用<br>
setTimeout() 的线程结束之前，函数或代码片段不能被执行。例如：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">foo</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&quot;foo 被调用&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">setTimeout</span>(foo, <span class="number">0</span>);</span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&quot;setTimeout 之后的代码&quot;</span>);</span><br></pre></td></tr></table></figure>
<p>会在控制台输出：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">setTimeout 之后的代码</span><br><span class="line">foo 被调用</span><br></pre></td></tr></table></figure>
<p>出现这个结果的原因是，尽管 setTimeout 以 0ms<br>
的延迟来调用函数，但这个任务已经被放入了队列中并且等待下一次执行；并不是立即执行；队列中的等待函数被调用之前，当前代码必须全部运行完毕，因此这里运行结果并非预想的那样。</p>
]]></content>
      <categories>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>vue-router中使用i18n来进行网页标题的国际化</title>
    <url>/Vue/vue-router-i18n-html-title/</url>
    <content><![CDATA[<p>思路很简单，在完成<code>vue-i18n</code>的安装后，第一次访问页面时候缓存默认语言，更改语言时，把当前语言缓存起来。在 i18n 中定义好不同语言的标题，然后在路由中引用作为 meta.title。在路由后置守卫中进行网页标题的修改。</p>
<p>代码：</p>
<p>首先完成 vue-i18n 配置：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; createI18n &#125; <span class="keyword">from</span> <span class="string">&quot;vue-i18n&quot;</span></span><br><span class="line"><span class="keyword">import</span> zh <span class="keyword">from</span> <span class="string">&quot;./zh&quot;</span></span><br><span class="line"><span class="keyword">import</span> en <span class="keyword">from</span> <span class="string">&quot;./en&quot;</span></span><br><span class="line"><span class="keyword">const</span> i18n = <span class="title function_">createI18n</span>(&#123;</span><br><span class="line">	<span class="attr">legacy</span>: <span class="literal">false</span>,</span><br><span class="line">	<span class="attr">locale</span>: <span class="string">&quot;zh&quot;</span>, <span class="comment">// 语言标识（缓存里面没有就用中文）</span></span><br><span class="line">	<span class="attr">messages</span>: &#123;</span><br><span class="line">		zh,</span><br><span class="line">		en,</span><br><span class="line">	&#125;,</span><br><span class="line">&#125;)</span><br><span class="line"><span class="keyword">if</span> (</span><br><span class="line">	<span class="variable language_">window</span>.<span class="property">localStorage</span>.<span class="title function_">getItem</span>(<span class="string">&quot;lang&quot;</span>) !== <span class="string">&quot;zh&quot;</span> &amp;&amp;</span><br><span class="line">	<span class="variable language_">window</span>.<span class="property">localStorage</span>.<span class="title function_">getItem</span>(<span class="string">&quot;lang&quot;</span>) !== <span class="string">&quot;en&quot;</span></span><br><span class="line">) &#123;</span><br><span class="line">	<span class="variable language_">window</span>.<span class="property">localStorage</span>.<span class="title function_">setItem</span>(<span class="string">&quot;lang&quot;</span>, <span class="string">&quot;zh&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line">i18n.<span class="property">global</span>.<span class="property">locale</span>.<span class="property">value</span> = <span class="variable language_">window</span>.<span class="property">localStorage</span>.<span class="title function_">getItem</span>(<span class="string">&quot;lang&quot;</span>)</span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> i18n</span><br></pre></td></tr></table></figure>
<p>en.js:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> &#123;</span><br><span class="line">	<span class="attr">meg</span>: &#123;</span><br><span class="line">		<span class="attr">language</span>: <span class="string">&quot;中文&quot;</span>,</span><br><span class="line">		<span class="attr">home</span>: <span class="string">&quot;Home&quot;</span>,</span><br><span class="line">		<span class="attr">category</span>: <span class="string">&quot;Category&quot;</span>,</span><br><span class="line">		<span class="attr">tags</span>: <span class="string">&quot;Tags&quot;</span>,</span><br><span class="line">		<span class="attr">test</span>: <span class="string">&quot;Test&quot;</span>,</span><br><span class="line">		<span class="attr">articles</span>: <span class="string">&quot;Articles&quot;</span>,</span><br><span class="line">		<span class="attr">album</span>: <span class="string">&quot;Album&quot;</span>,</span><br><span class="line">		<span class="attr">finNew</span>: <span class="string">&quot;Latest Articles&quot;</span>,</span><br><span class="line">	&#125;,</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>zh.js:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> &#123;</span><br><span class="line">	<span class="attr">meg</span>: &#123;</span><br><span class="line">		<span class="attr">language</span>: <span class="string">&quot;English&quot;</span>,</span><br><span class="line">		<span class="attr">home</span>: <span class="string">&quot;主页&quot;</span>,</span><br><span class="line">		<span class="attr">category</span>: <span class="string">&quot;分类&quot;</span>,</span><br><span class="line">		<span class="attr">tags</span>: <span class="string">&quot;标签&quot;</span>,</span><br><span class="line">		<span class="attr">test</span>: <span class="string">&quot;测试&quot;</span>,</span><br><span class="line">		<span class="attr">articles</span>: <span class="string">&quot;文章&quot;</span>,</span><br><span class="line">		<span class="attr">album</span>: <span class="string">&quot;相册&quot;</span>,</span><br><span class="line">		<span class="attr">finNew</span>: <span class="string">&quot;最新文章&quot;</span>,</span><br><span class="line">	&#125;,</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>router.js</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">//路由中的定义方式</span></span><br><span class="line">  &#123;</span><br><span class="line">    <span class="attr">path</span>: <span class="string">&#x27;/&#x27;</span>,</span><br><span class="line">    <span class="attr">name</span>: <span class="string">&#x27;home&#x27;</span>,</span><br><span class="line">    <span class="attr">component</span>: <span class="title class_">HomeView</span>,</span><br><span class="line">    <span class="attr">meta</span>: &#123;</span><br><span class="line">      <span class="attr">title</span>: i18n.<span class="property">global</span>.<span class="title function_">t</span>(<span class="string">&#x27;meg.home&#x27;</span>)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"><span class="comment">//...</span></span><br><span class="line"><span class="comment">//后置守卫</span></span><br><span class="line">router.<span class="title function_">afterEach</span>(<span class="function">(<span class="params">to, <span class="keyword">from</span></span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="variable language_">document</span>.<span class="property">title</span> = (i18n.<span class="property">global</span>.<span class="property">locale</span>.<span class="property">value</span> === <span class="string">&#x27;zh&#x27;</span>) ? <span class="string">&#x27;Hibi 博客站&#x27;</span> : <span class="string">&#x27;Hibi Blog&#x27;</span> + <span class="string">&#x27;-&#x27;</span> + to.<span class="property">meta</span>.<span class="property">title</span></span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Vue</category>
      </categories>
      <tags>
        <tag>Vue Router</tag>
        <tag>I18n</tag>
      </tags>
  </entry>
</search>
